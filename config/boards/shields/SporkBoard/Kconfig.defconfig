if SHIELD_SPORKBOARD

# Name must be less than 16 characters long!
    config SPORKBOARD
        default "Spork Board"
    
    config I2C
		default y

    config GPIO_MCP230XX
        default y

endif